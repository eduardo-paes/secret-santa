import React from 'react';
import ReactDOM from 'react-dom/client';
import AmigoOculto from './index';
import './src/index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AmigoOculto />
  </React.StrictMode>
);
